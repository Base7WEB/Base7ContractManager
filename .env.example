VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Usadas apenas server-side (Vercel Serverless Functions), NUNCA no bundle do frontend.
# Configurar via `vercel env` / painel da Vercel, nunca commitar valores reais.
SUPABASE_SERVICE_ROLE_KEY=
